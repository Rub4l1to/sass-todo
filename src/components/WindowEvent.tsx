import React from 'react';

// Icons
import { FaPlus } from 'react-icons/fa';

const WindowEvent = () => {
  return (
    <div className="window-event">
      <figure className="window-event__icon window-event__icon--plus">
        <FaPlus />
      </figure>
    </div>
  );
};

export default WindowEvent;
